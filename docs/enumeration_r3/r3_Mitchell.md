### MITCHELL COUNTY, NC (26 net-new)

| Source | Exact query-ready URL | Yield (counts + key fields) | Access | Effort | Why it matters |
|---|---|---|---|---|---|
| **Sales layer (round 1 missed entirely)** | `https://mapping.mitchellcountync.gov/arcgis/rest/services/WebMap2020/MapServer/18/query?where=1%3D1&outFields=PIN,sale_date,sale_price&returnGeometry=false&f=json` | **397** rows, `PIN` + `sale_date` + `sale_price` ($60,000.00 … $450,000.00). Range 2021-01-05 → 2021-12-30 (CY2021 only). Identical copy at `WebMap/MapServer/18`. | OPEN | XS | Only free source of **recorded consideration** in Mitchell. Round 1 enumerated only `WebMapNew` (which has no Sales layer) and concluded no comp spine existed. PIN joins straight to the parcel layer. |
| Parcels — probate/heir segment | `…/WebMapNew/MapServer/12/query?where=UPPER(Owner1)+LIKE+'%25HEIR%25'&outFields=PIN,Owner1,LocAddr,MailAddr,MailCity,MailState,MailZip,Total,Deed_Date&returnGeometry=false&f=json` | **392** heir-owned parcels w/ full mailing address (`WILLIS ROY ALLEN HEIRS`, `HUSKINS BERTHA HEIRS`). +117 `%ESTATE%`, +683 `%ET AL%`. | OPEN | XS | Heir/probate is a top-tier motivated-seller source; each row already carries name→property→mailing contact. |
| Parcels — probate **deed reference** | `…/WebMapNew/MapServer/12/query?where=UPPER(Deed_Ref)+LIKE+'%25WILL%25'&outFields=PIN,Owner1,Deed_Ref,DeedBook,Deed_Date,LocAddr,Total&returnGeometry=false&f=json` | **97** parcels acquired by will (`Deed_Ref` LIKE WILL); 74 more `DeedBook` LIKE `PER`. HCCOG copy exposes the **estate file number** (`WILL/16E52`, `WILL/15E177`). | OPEN | XS | Estate file numbers (`16E52` = 2016-E-52) link a parcel directly to a Clerk of Superior Court estate file — a court-lane join the engine has no other way to make in Mitchell. |
| Parcels — long-hold / elderly proxy | `…/WebMapNew/MapServer/12/query?where=Deed_Date+<+DATE+'1990-01-01'+AND+Dwelling>0&outFields=PIN,Owner1,LocAddr,MailAddr,Total,Deed_Date&returnGeometry=false&f=json` | **957** improved parcels held since before 1990 (1,599 all classes; 894 pre-1980). | OPEN | XS | Tenure length is the strongest free elderly/equity proxy where no DOB source exists. |
| Parcels — distressed structure | same layer, `where=Total>0 AND Total<20000 AND Dwelling>0` | **261** improved parcels assessed under $20k. | OPEN | XS | Assessed-value floor on an improved parcel is a direct condition signal. |
| Parcels — entity + absentee cross | same layer, `where=UPPER(Owner1) LIKE '%LLC%' AND MailState<>'NC'` | **649** out-of-state LLC-owned; 2,267 entity-owned total; **3,559** in-NC but out-of-county mailing (on top of round 1's 3,621 out-of-state). | OPEN | XS | Round 1 counted only out-of-state. Total absentee is ~7,180, roughly double what was reported. |
| Parcels — government surplus | same layer, `where=UPPER(Owner1) LIKE '%MITCHELL COUNTY%'` / `LIKE '%TOWN OF%'` | **66** county-owned + **52** municipal-owned (`TOWN OF SPRUCE PINE`, incl. $994k and $386.9k parcels). | OPEN | XS | Mitchell publishes no surplus-property page (verified); this layer *is* the surplus inventory. |
| Voluntary Ag District layer | `…/WebMapNew/MapServer/14/query?where=1%3D1&outFields=PIN,Owner1,MailAddr,MailCity,MailState,MailZip,LegalAc,LocAddr,DeedBook,DeedPage,Deed_Date&returnGeometry=false&f=json` | **101** parcels, own copy of owner + mailing + acreage + deed. | OPEN | XS | Closest available proxy to present-use/deferred value — VAD enrollment signals rollback-tax exposure on sale. The true `presentval` field is blank everywhere (see negatives). |
| **Encumbrance layer — round 1 verdict wrong** | `…/WebMapNew/MapServer/9/query?where=Name+LIKE+'0%25-00-%25'&outFields=Name,Notes&returnGeometry=false&f=json` | **1,414** rows, **1,080** with PIN-shaped `Name`. `Name` **is the PIN**, `Notes` = deed book/page/tract (`273/436/TR4`). Join verified: PIN `0882-00-52-1389` → `PENLAND BAILEY ESTATE LLC`, 807 BEAR CREEK RD, $5,617,100. | OPEN | XS | Round 1 called it "no owner, no amount, no party. Low value." It is fully joinable to the parcel roll — an encumbrance flag on 1,080 parcels. |
| **HCCOG ArcGIS Online org — round 1 said none exists** | `https://hccog.maps.arcgis.com/sharing/rest/search?q=orgid%3Avj28eVZMB2OMIUh5%20AND%20Mitchell&num=100&f=json` | orgId `vj28eVZMB2OMIUh5`, owner `HCCOGGIS`. **20** Mitchell items, **9** Spruce Pine items, all `public`. | OPEN | XS | Round 1's sweep 2 concluded "Mitchell County NC has **no ArcGIS Online organization** … the Henderson-style hiding place does not exist here." It exists — it is just held by the **regional COG**, not the county. |
| HCCOG hosted FeatureServer root | `https://services1.arcgis.com/vj28eVZMB2OMIUh5/arcgis/rest/services?f=json` | **114 services** — no page links most of them. Mitchell-relevant: `Mitchell_County_Parcels`, `SiteStructureAddressPoints_SprucePineSelection`, `Spruce_Pine_Zoning`, `Spruce_Pine_Town_Limits`, `Spruce_Pine_Flood_Hazard_Zones`, `SprucePine_StreamCenterlines`, `Road_Centerlines`. Also neighbor rolls (`Avery_County_Parcels`, `Wilkes_County_Parcels`, `YanceyCountyBaseData`, `AsheCountyParcels_201706`). | OPEN | S | Entirely new host for the engine. The neighbor-county parcel rolls are free footprint expansion. |
| HCCOG Mitchell parcels (dated snapshot) | `https://services1.arcgis.com/vj28eVZMB2OMIUh5/arcgis/rest/services/Mitchell_County_Parcels/FeatureServer/0/query?where=1%3D1&outFields=PARNO,OWNNAME,SITEADD,MAILADD,MCITY,MSTATE,MZIP,LANDVAL,IMPROVVAL,PARVAL,SOURCEREF,SOURCEDATE,GISACRES,STRUCT&returnGeometry=false&f=json` | **4,557** rows, service name `Parcels_20251007`. 390 out-of-state, 1,510 land-only, 286 improved <$20k, 91 `OWNNAME` HEIRS, 23 `SOURCEREF` WILL, 604 `SOURCEDATE` pre-1990. | OPEN | S | A **dated (2025-10-07) snapshot** against the county's live layer — diffing the two yields ownership changes without any deed feed. Also carries `SOURCEREF` estate file numbers the county layer abbreviates. |
| Spruce Pine Municipal Data Viewer (app + webmap) | `https://hccog.maps.arcgis.com/sharing/rest/content/items/2eb3620582644682a89556b86fabd1ca/data?f=json` | 8 operational layers, all URLs resolved. Search config exposes `OWNNAME` owner-name search over the parcel layer. | OPEN | XS | The item that reveals the whole HCCOG host. **No `definitionExpression` on any layer** — see negatives. |
| Spruce Pine zoning (rezoning + annexation dates) | `https://services1.arcgis.com/vj28eVZMB2OMIUh5/arcgis/rest/services/Spruce_Pine_Zoning/FeatureServer/0/query?where=1%3D1&outFields=ZoneCode,ZoneDesc,RezoningDate,PreviousZone,AnnexationDate,AONumber&returnGeometry=false&f=json` | **244** polygons w/ `RezoningDate`, `PreviousZone`, `AnnexationDate`, `AONumber`. | OPEN | XS | Recent rezoning/annexation is a redevelopment-pressure signal; the county's own `Spruce Pine Zoning` layer (WebMapNew/8, 242 rows) has only `Zoning_11` + acreage. |
| Spruce Pine address points | `https://services1.arcgis.com/vj28eVZMB2OMIUh5/arcgis/rest/services/SiteStructureAddressPoints_SprucePineSelection/FeatureServer/0/query?where=1%3D1&outFields=FullAddres,Status,Add_Number,St_Name,Post_Comm,Post_Code&returnGeometry=false&f=json` | **1,598** points w/ a `Status` field (county layer 1 has no status). | OPEN | XS | `Status` distinguishes active vs retired addresses — a demolition/vacancy hint absent from the county's 11,571-point layer. |
| **NC OneMap statewide parcels (token-free)** | `https://services.nconemap.gov/secure/rest/services/NC1Map_Parcels/MapServer/0/query?where=CNTYNAME%3D%27Mitchell%27&outFields=parno,ownname,ownfrst,ownlast,mailadd,mcity,mstate,mzip,siteadd,landval,improvval,parval,sourceref,sourcedate,transfdate,gisacres,struct&returnGeometry=false&f=json` | **17,671** Mitchell parcels (county's own layer has 17,663). 3,782 out-of-state, 391 HEIRS, 91 `sourceref` WILL, 8,178 land-only. Parsed `ownfrst`/`ownlast`. No token required. | OPEN | S | Independent second copy of the entire roll on a state host — survives county-server outages, and `ownfrst`/`ownlast` are pre-split for name matching. |
| NC OneMap bulk download (whole roll) | `https://www.nconemap.gov/api/download/v1/items/943c5690291445c4bb679a7422dd8b93/csv?layers=0` (also `/shapefile`, `/geojson`, `/kml`; polygons item `1de3d7d828ce4813b838ddf055b40317`) | Returns HTTP **202** `{"status":"Pending"}` — async generation, re-poll to collect. Statewide CSV/ZIP/GeoJSON of the parcel roll. | OPEN | M | The bulk-download ask, satisfied at state level. Two-step async, not a single GET. |
| NC OneMap WFS (alt bulk path) | `https://services.nconemap.gov/secure/services/NC1Map_Parcels/MapServer/WFSServer?request=GetCapabilities&service=WFS` | HTTP 200, valid WFS 2.0 capabilities. WMS also published. | OPEN | M | Paged WFS avoids the 5,000-record `maxRecordCount` ceiling on the REST layer. |
| NC OneMap DCAT catalog | `https://www.nconemap.gov/api/feed/dcat-us/1.1.json` | **459** datasets; parcel entries carry every distribution URL above. | OPEN | XS | The machine-readable index that exposes the download endpoints — `nconemap.gov/data.json` 301-redirects here. |
| High Country Absentee Ownership Study | `https://services2.arcgis.com/wdQEqhSQSuYA89VW/arcgis/rest/services/North_Carolina_High_Country_Absentee_Ownership_Study_Variables/FeatureServer/0/query?where=1%3D1&outFields=*&returnGeometry=false&f=json` | **50 census tracts** across Alleghany/Ashe/Avery/**Mitchell**/Watauga/Wilkes/Yancey w/ % absentee ownership + vulnerability benchmarks. Modified 2021. | OPEN | XS | Tract-level scoring overlay only — **not** a parcel list. Useful to weight the 7,180 absentee parcels, not to generate them. |
| **Town of Spruce Pine — live domain** | `https://www.sprucepine-nc.gov/publicnotices` (also `/ordinances`, `/bidspropsals`, `/formsandpermits`, `/buildingplanningzoning`) | Live municipal site. `townofsprucepine.com` 302s here; **`townofsprucepine.org` is dead (000)**. Content is JS-rendered (membershipware SPA) — nav only in raw HTML. | OPEN-HTML | M | The municipal layer for the county's largest town. Requires a JS-capable fetch to read notice bodies. |
| Town of Bakersville — live domain | `https://www.bakersvillenc.com/government.html` | Live static site (`administration.html`, `government.html`, `police-1.html`). **`townofbakersville.com` is dead (000)**. | OPEN-HTML | S | Second municipality; county seat. No condemned/nuisance list published. |
| GovDeals — Town of Spruce Pine seller | `https://www.govdeals.com/sprucepine` | Dedicated seller page linked from every Spruce Pine page. Returns **403 to curl** — listing count **UNVERIFIED**. | OPEN-HTML (403 to automated fetch) | S | Round 1 searched GovDeals for "Mitchell County NC" and found nothing, concluding DEAD. The seller is registered under the **town**, not the county. Needs the existing `curl_cffi impersonate` path used for the Akamai-walled GovDeals lane. |
| ZoomProspector commercial sites (via EDC) | `https://properties.zoomprospector.com/northcarolina?page=1&s%5BGeoEntityList%5D=24fca562-5160-4c93-a71a-e882b146d0ce&s%5BSortBy%5D=featured&s%5BSizeUnits%5D=1` | HTTP 200, **"0 Property Results / 0 total Properties"** for the Mitchell geo-entity. | OPEN | XS | Registered and queryable but currently empty — worth a low-frequency re-poll, not a build. |
| Tax roll UNPAID filter (re-confirmed wall) | `https://secure.webtaxpay.com/index.php?nav=taxrecords&county=mitchell&state=NC` → POST `search.php` w/ `search[status]=UNPAID&search[type]=R` | Form exposes `search_by` = name/billNo/accountNo/mapNo, `status` = UNPAID/PAID, `type` = R/P. POST **with** a live session cookie + Referer + browser UA still returns **HTTP 403 "We could not verify that you are human."** `mitchell.webtaxpay.com` 302s to the same host. | MANUAL (bot-verification wall — not bypassed) | M | The taxes-owed gap for Mitchell. Belongs in the existing saved-page manual lane. |
| Assessor portal (Aumentum) | `https://nc-mitchell.publicaccessnow.com/Assessor/PropertySearch.aspx` | Parcel/address/owner search. Module scan: only `Assessor.aspx`, `Assessor/PropertySearch.aspx`, `Home.aspx` return 200; `Treasurer`/`Tax`/`Collections`/`Recorder`/`Land`/`Appeals`/`Personal` all **404**. | OPEN-HTML | M | Confirms there is no collections module — the tax-owed dollar figure is genuinely not on this portal. |

**Negatives worth recording so nobody re-probes:** all five parcel services (`WebMapNew`, `WebMapNew2023`, `WebMap2020`, `WebMap`, `EnergovWebMap2`) return the **same 17,663** rows — there is no multi-vintage county snapshot. `presentval`, `owntype`, `saledate`, `structyear` exist in the NC-standard schema on both HCCOG and NC OneMap but are **blank for all 17,671 Mitchell rows** (my first count of 17,671 was an artifact of testing `<>' '` against `''` — corrected). **No `definitionExpression`** on any layer of the HCCOG webmap, and no Dashboard/Experience/StoryMap for Mitchell — the Henderson-style curated list does not exist here even on the COG host. No per-year delinquent services anywhere. Kania Law Firm publishes **0 Mitchell** rows (`kanialawfirm.com/tax-foreclosures/`, `/foreclosure-listings/`). County WordPress: 712 posts, 104 pages, 535 attachments — **no** delinquent/foreclosure/surplus list; `mitchellcounty.org` returns **523** (dead). `mapping.mitchellcountync.gov/maps/` bundle contains **no** 32-hex item ids and **no** external hosts (round 1 correct). DEAD vendor probes: `mitchellnc.spatialest.com`, `mitchellcountync.revize.com`, `mitchell.acclaimweb.com`, `rod.mitchellcountync.gov`, `municipalonlinepayments`, `mitchell.courthousecomputersystems.com` (wildcard redirect to corporate, not a tenancy), `ecode360` (Cloudflare 403), plus round 1's DEVNET/Cott/Kofile/qPayBill/Catalis/Zuercher/lrcpwa/bcpwa/bttaxpayerportal/PoliceToCitizen.

**Sweep 7 gap diff — two live bugs found:**
- `/Users/cashhigh/foreclosure-scraper/src/foreclosure_scraper/scrapers/city_websites/cities.py:134-135` registers **both** Mitchell municipalities on dead domains: `("NC","Mitchell","Bakersville","townofbakersville.com")` and `("NC","Mitchell","Spruce Pine","townofsprucepine.org")` — both return 000. Live hosts are `bakersvillenc.com` and `sprucepine-nc.gov`.
- `/Users/cashhigh/foreclosure-scraper/src/foreclosure_scraper/enrichment_parcel_from_geo.py:57-60` pins NC OneMap to `NC1Map_Parcels/FeatureServer/1` for reverse-geo, with a documented note that point queries silently return zero features. Attribute queries work on **both** `FeatureServer/1` and `MapServer/0` (17,671 each) — the layer is usable as a bulk Mitchell roll even though the geo path is broken.
- Round 1's third finding still stands: `enrichment_arcgis.py:128-136` registers `mapping.mitchellcountync.gov` as address-lookup only, declaring just `addr_field` while `Owner1`, `MailAddr`, `Total`, `Deed_Ref` and `Deed_Date` sit unharvested in the same response.

**Top 5 net-new:**
1. **HCCOG ArcGIS Online org + its 114-service hosted root** (`services1.arcgis.com/vj28eVZMB2OMIUh5`) — reverses round 1's flat "no ArcGIS Online organization" verdict, adds a dated parcel snapshot for change-detection, and hands over free neighbor-county rolls (Avery, Wilkes, Yancey, Ashe).
2. **The Sales layer** (`WebMap2020/MapServer/18`) — 397 PIN-keyed sale prices, the only free recorded-consideration source in the county, invisible to a `WebMapNew`-only sweep.
3. **Probate/heir segmentation** — 392 HEIRS + 97 `Deed_Ref` WILL, with HCCOG's `SOURCEREF` supplying the actual estate file numbers (`16E52`) that link a parcel to a Clerk of Court file.
4. **NC OneMap token-free statewide roll + async bulk download** — independent 17,671-row copy plus CSV/ZIP/GeoJSON/WFS bulk paths.
5. **The municipal layer** — `sprucepine-nc.gov` and `bakersvillenc.com` are live while both domains in `cities.py` are dead, and the GovDeals surplus seller is registered under the **town**, not the county.

**Biggest remaining gap:** per-parcel dollars owed — `secure.webtaxpay.com` holds the UNPAID roll behind a human-verification wall, the §105-369 lien advertisement runs only in the newspaper, the Aumentum portal has no collections module, and `presentval`/`saledate`/`structyear` are blank in every free GIS copy, so tax debt, year built and post-2021 sale price all remain unobtainable without the manual saved-page lane.

**Coverage score:** 8/10